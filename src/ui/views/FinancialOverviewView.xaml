<UserControl x:Class="ui.views.FinancialOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300">
    <DataGrid ItemsSource="{Binding FinancialData}">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Date" Binding="{Binding Date}"/>
            <DataGridTextColumn Header="Income" Binding="{Binding Income}"/>
            <DataGridTextColumn Header="Spending" Binding="{Binding Spending}"/>
            <DataGridTextColumn Header="Savings" Binding="{Binding Savings}"/>
            <DataGridTextColumn Header="Dividends" Binding="{Binding Dividends}"/>
            <DataGridTextColumn Header="Appreciation" Binding="{Binding Appreciation}"/>
            <DataGridTextColumn Header="Change In Net Worth" Binding="{Binding ChangeInNetWorth}"/>
            <DataGridTextColumn Header="Net Worth" Binding="{Binding NetWorth}"/>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
